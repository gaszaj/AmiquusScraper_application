import { en } from "./en";
import { fr } from "./fr";
import { de } from "./de";
import { es } from "./es";
import { it } from "./it";

export type Language = "en" | "fr" | "de" | "es" | "it";

export const translations = {
  en,
  fr,
  de,
  es,
  it
};
